{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "serve": "npm run dev --workspace server",
    "lint": "eslint .",
    "format": "prettier --write .",
    "lingui:extract": "npm run lingui:extract --workspace client",
    "lingui:compile": "npm run lingui:compile --workspace client",
    "codegen": "npm run codegen --workspace client"
  },
  "dependencies": {
    "vscode-ws-jsonrpc": "^3.4.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "@types/node": "^24.0.13",
    "eslint": "^9.31.0",
    "eslint-config-prettier": "^10.1.5",
    "prettier": "3.6.2",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.30.1"
  }
}
